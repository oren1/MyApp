name: build-ios-app
on:
  push:
    branches:
      - 'main'
jobs:
  build:
    runs-on: macos-latest
    steps:
      - run: echo 'Hi ${{ vars.EXAMPLE_VARIABLE }}'
      - run: echo 'Hi ${{ secrets.DEVELOPER_APP_IDENTIFIER }}'