name: build-ios-app
on:
  push:
    branches:
      - 'master'
jobs:
  build:
    runs-on: macos-latest
    steps:
      - run: echo 'Hi ${{ env.EXAMPLE_VARIABLE }}'
      - run: echo 'Hi ${{ secrets.DEVELOPER_APP_IDENTIFIER }}'